import 'package:flutter/material.dart';
import 'package:midterm_mobile/main.dart';

class HistoryPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return MyScaffold(route: '/history', body: Text('history'));
  }
}